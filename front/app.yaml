runtime: python312

handlers:

  - url: /static
    static_dir: static

  - url: /.*
    static_files: index.html
    upload: index.html

# Variables d'environnement (optionnel)
# env_variables:
#   MY_VAR: "ma_valeur"

# Scaling automatique (optionnel — valeurs par défaut App Engine)
# automatic_scaling:
#   min_instances: 0
#   max_instances: 2
#   target_cpu_utilization: 0.65
